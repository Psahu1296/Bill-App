/**
 * Firestore sync removed — backend now deployed to Railway.
 * Stubs kept so TypeScript doesn't break during the import cleanup transition.
 * TODO: remove all call sites then delete this file.
 */
export function startFirestoreSync() {}
export function pushOrderStatusToFirestore(_id: unknown, _update: unknown) {}
export async function pushConfigToFirestore() {}
